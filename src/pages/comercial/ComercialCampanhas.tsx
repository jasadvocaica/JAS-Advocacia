import { useQuery } from "@tanstack/react-query";
import { Megaphone } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { Card } from "@/components/ui/card";
type Campanha={id:string;nome:string;canal:string|null;area_direito:string|null;status:string;orcamento_total:number|null;gasto_realizado:number|null;leads_gerados:number|null};
const moeda=(v:number)=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(v);
export default function ComercialCampanhas(){const{data:items=[],isLoading}=useQuery({queryKey:["mkt-campanhas"],queryFn:async()=>{const{data,error}=await(supabase as any).from("mkt_campanhas").select("id,nome,canal,area_direito,status,orcamento_total,gasto_realizado,leads_gerados").order("criado_em",{ascending:false});if(error)throw error;return(data??[]) as Campanha[];}});return <div className="space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">Comercial</p><h1 className="font-display text-3xl">Campanhas e origens</h1><p className="text-sm text-muted-foreground">Resultados baseados nos registros do CRM.</p></div>{isLoading?<p>Carregando…</p>:items.length===0?<Card className="p-10 text-center"><Megaphone className="mx-auto h-10 w-10 text-primary/40"/><h2 className="mt-3 font-display text-2xl">Nenhuma campanha cadastrada</h2><p className="mt-2 text-sm text-muted-foreground">Os indicadores aparecerão quando houver campanhas e leads reais registrados.</p></Card>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(c=><Card key={c.id} className="p-5"><div className="flex justify-between gap-3"><h2 className="font-display text-xl">{c.nome}</h2><span className="text-xs text-muted-foreground">{c.status}</span></div><p className="mt-1 text-sm text-muted-foreground">{c.canal||"Canal não informado"} · {c.area_direito||"Todas as áreas"}</p><div className="mt-5 grid grid-cols-3 gap-2 text-sm"><div><p className="text-xs text-muted-foreground">Orçamento</p><p className="font-medium">{moeda(Number(c.orcamento_total||0))}</p></div><div><p className="text-xs text-muted-foreground">Gasto</p><p className="font-medium">{moeda(Number(c.gasto_realizado||0))}</p></div><div><p className="text-xs text-muted-foreground">Leads</p><p className="font-medium">{c.leads_gerados||0}</p></div></div></Card>)}</div>}</div>}